---
import BaseLayout from './BaseLayout.astro';
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, date, tags, image } = post.data;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<BaseLayout title={title} description={description} image={image}>
  <article class="pt-24 md:pt-28 pb-16">
    <div class="container max-w-3xl">
      <!-- Post Header -->
      <header class="mb-12">
        <div class="flex flex-wrap gap-2 mb-4">
          {tags.map((tag) => (
            <span class="px-3 py-1 text-xs font-medium uppercase tracking-wider
                         bg-[var(--color-primary)] text-white rounded-full">
              {tag}
            </span>
          ))}
        </div>

        <h1 class="text-4xl md:text-5xl font-bold font-heading mb-4 leading-tight">
          {title}
        </h1>

        <div class="flex items-center gap-4 text-[var(--color-text-muted)]">
          <time datetime={date.toISOString()}>
            {formattedDate}
          </time>
        </div>
      </header>

      <!-- Featured Image -->
      {image && (
        <div class="mb-12 rounded-lg overflow-hidden">
          <img src={image} alt={title} class="w-full h-auto" />
        </div>
      )}

      <!-- Post Content -->
      <div class="prose prose-lg max-w-none blog-content
                  prose-headings:font-heading prose-headings:font-bold
                  prose-a:text-[var(--color-primary)] prose-a:no-underline hover:prose-a:underline
                  prose-code:bg-[var(--color-bg-alt)] prose-code:px-2 prose-code:py-1 prose-code:rounded
                  prose-pre:bg-[var(--color-bg-alt)] prose-pre:border prose-pre:border-[var(--color-border)]">
        <slot />
      </div>

      <!-- Back Link -->
      <div class="mt-16 pt-8 border-t border-[var(--color-border)]">
        <a href="/blog" class="btn btn-outline">
          &larr; Back to Blog
        </a>
      </div>
    </div>
  </article>
</BaseLayout>

<style>
  /* Theme-specific prose colors */
  .blog-content {
    --tw-prose-body: var(--color-text);
    --tw-prose-headings: var(--color-text);
    --tw-prose-links: var(--color-primary);
    --tw-prose-bold: var(--color-text);
    --tw-prose-code: var(--color-text);
    --tw-prose-quotes: var(--color-text-muted);
    --tw-prose-hr: var(--color-border);
    --tw-prose-bullets: var(--color-text-muted);
    --tw-prose-counters: var(--color-text-muted);
  }

  :global([data-theme="p3"]) .blog-content {
    --tw-prose-invert-body: var(--color-text);
    --tw-prose-invert-headings: var(--color-text);
  }
</style>
